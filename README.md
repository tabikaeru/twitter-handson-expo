This is expo template repository

1. You need to modify name using react-native-rename.
   Use below command
   `react-native-rename newAppName`

☠️Caution☠️
It's better to checkout repository, by
`git checkout -b rename-app`
